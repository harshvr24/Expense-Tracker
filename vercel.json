{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "frontend/build",
  "routes": [
    {
      "src": "^/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "^(?!/?api).*",
      "dest": "/index.html"
    }
  ]
}
